<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/red_header</item>
        <item name="colorPrimaryDark">@color/red_primary_header</item>
        <item name="colorAccent">@color/white</item>
    </style>
    <style name="AddRecipe" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/gray_header</item>
        <item name="colorPrimaryDark">@color/gray_primary_header</item>
    </style>
</resources>